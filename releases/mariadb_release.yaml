---
apiVersion: helm.integrations.flux.weave.works/v1alpha2
kind: FluxHelmRelease
metadata:
  name: mariadb
  namespace: test
  labels:
    chart: mariadb
  annotations:
    flux.weave.works/tag.chart-image: glob:10.*
spec:
  chartGitPath: mariadb
  values:
    image: bitnami/mariadb:_
    persistence:
      enabled: false
